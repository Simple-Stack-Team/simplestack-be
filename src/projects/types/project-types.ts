export enum ProjectPeriod {
  FIXED = 'Fixed',
  ONGOING = 'Ongoing',
}

export enum ProjectStatus {
  NOT_STARTED = 'Not Started',
  STARTING = 'Starting',
  IN_PROGRESS = 'In Progress',
  CLOSING = 'Closing',
  CLOSED = 'Closed',
}
